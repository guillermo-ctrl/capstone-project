spring:
  main:
    banner-mode: off
  datasource:
    url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1
    username: sa
    password: sa
security:
  jwt:
    secret: "super-secret"
    expires-after-minutes: 30