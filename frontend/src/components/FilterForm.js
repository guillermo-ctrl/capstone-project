import styled from 'styled-components/macro'

export default styled.form`
    
    display: block;
     justify-content: right;
     margin-left: 10%;
    
    input {
       padding: 5px;
       float: right;
       clear: both;
    }
    
    input[type=submit] {
       display: block;
       width: 300px;
       margin: 20px auto;
       padding: 10px;
       border: 0;
       color: white;
       font-size: 1em;
       background: #15aabf;
    }
  
    div {
       padding: 10px;
    }
    
     
    `
